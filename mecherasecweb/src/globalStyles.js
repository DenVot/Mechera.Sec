import { createUseStyles } from 'react-jss'

export const useDefStyles = createUseStyles({
    brandBackground: {
        background: "var(--mechera-green)"
    },
    fontBrand: {
        color: "var(--mechera-green)"
    }
});
